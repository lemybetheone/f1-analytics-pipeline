# Secrets — never commit
.env
.env.*
!.env.example

# Private context — local only, never published
*.private.md
# Skill-level calibration for the AI assistant; not for public/recruiter view
context/PROFILE.md
# AI assistant entry point — kept local, not published to the portfolio repo
/CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Run logs (backfill output; regenerable, and large)
logs/

# Airflow
airflow/logs/
airflow/plugins/__pycache__/
# Generated by Airflow on first run (3,000+ lines derived from the
# compose environment). Regenerated, not authored.
airflow/config/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
